const wall = function (item) {
    console.log("The wall has been painted red")
};

const left_bucket = "Yellow paint";
const right_bucket = "blue paint";

wall(left_bucket);
wall(right_bucket);

const color = function (Noord, zuid) {
    console.log(Noord + zuid)
};

color(oranje, grijs)